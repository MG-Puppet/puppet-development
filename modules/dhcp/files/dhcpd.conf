# Global Settings
option domain-name "mitchell.test";
option domain-name-servers 192.168.153.100;

default-lease-time 600;
max-lease-time 7200;

# The ddns-updates-style parameter controls whether or not the server will
# attempt to do a DNS update when a lease is confirmed. We default to the
# behavior of the version 2 packages ('none', since DHCP v2 didn't
# have support for DDNS.)
ddns-update-style none;

# Authoritative DHCP Server
authoritative;

# Use this to send dhcp log messages to a different log file (you also
# have to hack syslog.conf to complete the redirection).
#log-facility local7;

# LAN Net
subnet 192.168.153.0 netmask 255.255.255.0 {

        option routers 192.168.153.254;
        option subnet-mask 255.255.255.0;
        range 192.168.153.150 192.168.153.200;

        }
# DMZ Net
subnet 172.16.17.0 netmask 255.255.255.0 {
        option routers 172.16.17.254;
        option subnet-mask 255.255.255.0;
        range 172.16.17.10 172.16.17.20;
       }
